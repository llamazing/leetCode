class Solution {
public:
    void rotate(vector<vector<int>>& matrix) {
		int size = matrix.size();
		int top = 0, down = size - 1, left = 0, right = size - 1;
		int cycle = ( size + 1)/2;
		int i  = 0;
        
    }
};

